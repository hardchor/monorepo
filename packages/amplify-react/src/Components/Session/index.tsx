import React, { FunctionComponent } from 'react';
import { RouteComponentProps } from '@reach/router';

const Session: FunctionComponent<RouteComponentProps> = () => {
  return (
    <div>
      <h2>Session</h2>
    </div>
  );
};

export default Session;
